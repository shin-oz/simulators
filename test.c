#include <stdio.h>
#include <stdlib.h>
#include <time.h> 

int main(void){
/*int i=0;
	for(i=0;i<10;i++){
       	srand((unsigned)time(NULL));   //乱数の初期化を実行時間を元に実行
   int a = rand();	//代入時に強制的にキャスト変換
     int c = a%2;  //代入時に強制的にキャスト変換
	printf("%d\n",c);}
  


    int i;
    srand(10);
    for(i=0;i<100;i++){  printf("%d\n",rand()%2);  }

	return 0;

	srand((unsigned)time(NULL));
       int i = rand() % 2;
      printf("%d\n",i);
*/
	printf("%d\ni",X);
      return 0;
}

